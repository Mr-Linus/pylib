syntax = "proto3";

// The greeting service definition.
service Connect {
  // Sends a greeting
  rpc persent (Request) returns (ReplyInt) {}
}

// The request message containing the user's name.
message Request {
  string name = 1;
}

// The response message containing the greetings
message ReplyInt {
  int32 per = 1 ;
}
